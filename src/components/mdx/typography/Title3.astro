---
const { id } = Astro.props;
---

<h3 class="title3" id={id}><slot /></h3>

<style>
	.title3 {
		font-size: var(--font-size-lg);
		font-family: var(--font-sans);
		color: var(--flexoki-800);
		line-height: var(--leading-tight);
	}
</style>
